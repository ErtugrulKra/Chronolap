namespace Chronolap
{
    public enum LapStatisticsType
    {
        ArithmeticMean,
        Median,
        StandardDeviation,
        Min,
        Max,
        Variance,
        Percentile
    }
}
